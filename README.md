# taegis-detection-analysis
Process a CSV file containing Taegis XDR detections to analyze relationships between sensor types, alert severity, and final status. Generate a Sankey diagram visualizing the flow: sensor_types → severity → status.
